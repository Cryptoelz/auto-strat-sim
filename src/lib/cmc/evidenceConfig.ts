/**
 * Stage 3D — feature flag for the CMC Evidence Summary page.
 * Setting this to false removes the route, the nav entry and the page surface.
 * Nothing else in the application depends on it.
 */
export const CMC_EVIDENCE_ENABLED = true;
